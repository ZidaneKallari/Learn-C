#include <stdio.h>
#include <string.h>

int main()
{
    if (!(strcmp("makan", "makan") == 0))
    {
        printf("Kondisi ini tidak terpenuhi");
    }
    else
    {
        printf("Kondisi ini terpenuhi");
    }
    return 0;
}
